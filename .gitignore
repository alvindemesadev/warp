.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust build output (very large, not needed in repo)
src-tauri/target/

# Built installers (generated by `npm run build:win`; download from the release/website)
docs/*.exe
docs/*.msi

# Temp build script
build.bat
scripts/_build_tmp.bat

# Updater manifest (generated per-release by scripts/updater-manifest.js and uploaded to the GitHub release)
latest.json

# Landing site has its own repository (alvindemesadev/warp-site)
warp-site/

# Local session tooling (never commit)
.freebuff/

